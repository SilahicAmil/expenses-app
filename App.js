import { StyleSheet, Text, View } from "react-native";

import ExpensesNavigator from "./navigators/expenses-navigator";

export default function App() {
  return <ExpensesNavigator />;
}

const styles = StyleSheet.create({});
